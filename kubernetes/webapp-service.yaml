apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    name: app-service
    app: my-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000
  selector: 
    name: web-app-pod
    app: my-app
